# Celebal_Assignment_1
"Objective: Learn Python basics and perform basic data exploration and cleaning using Pandas. Steps: 1.Load a CSV dataset into a Pandas DataFrame. 2.Explore data (head/tail, shape, columns, data types). 3.Handle missing values (identify, fill/drop). 4.Perform basic operations (filter rows, select columns). 5.Remove duplicates. 6.Create a derived column (total_amount = price * quantity). 7.Save the cleaned dataset as a new CSV file. Output: Jupyter Notebook (.ipynb) + cleaned CSV + brief summary."

Resources :
https://www.kaggle.com/datasets/anvitkumar/shopping-dataset


# Summary

- Loaded Combined_dataset.csv using Pandas
- Explored dataset using head(), tail(), shape, columns, and dtypes
- Checked and handled missing values
- Removed duplicate rows
- Filtered products with rating greater than 4
- Selected important columns like title, rating, and final_price
- Created derived column total_amount
- Saved cleaned dataset as cleaned_dataset.csv
